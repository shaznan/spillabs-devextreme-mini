import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  MainHeading: {
    display: "flex",
    justifyContent: "center",
  },

  container: {
    paddingTop: "4rem",
  },

  root: {
    flexGrow: 1,
  },
});

export { useStyles };
